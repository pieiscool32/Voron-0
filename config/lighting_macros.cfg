[gcode_macro PANEL_IDLE]
gcode:
  SET_LED_EFFECT EFFECT=idle REPLACE=1

[gcode_macro PANEL_NIGHT]
gcode:
  SET_LED_EFFECT EFFECT=night REPLACE=1

[gcode_macro LIGHTS_OFF]
gcode:
  STOP_LED_EFFECTS
  SET_LED_EFFECT EFFECT=heating